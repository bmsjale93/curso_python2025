# Verifica si un número es positivo y no es cero.

# Creamos la variable y pedimos un número al usuario
numero = int(input("Introduce un número: "))

# Creamos la condición
if numero > 0:
    print("Ha pasado el control.")
else:
    print("El número NO ha pasado el control.")