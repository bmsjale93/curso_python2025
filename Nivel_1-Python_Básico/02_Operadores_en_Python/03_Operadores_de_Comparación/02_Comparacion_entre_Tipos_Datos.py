# Comparación entre Tipos de Datos
# Python permite comparar distintos tipos de datos, pero hay reglas que se deben respetar:

print(5 == "5") # False (int vs str)
print(3.0 == 3) # True (float vs int)

# En general, debemos evitar comparar tipos diferentes para evitar errores.